# Custom Rules

- **Deployment Platform:** For all front-end/website deployments of this project, we have migrated from Netlify to Cloudflare. Do not deploy to Netlify; only deploy to Cloudflare Pages (e.g. using `npx wrangler pages deploy`).
